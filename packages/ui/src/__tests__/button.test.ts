import { describe, it, expect } from 'vitest';
import { Button } from '../button';

describe('@studioflow/ui Button', () => {
  it('is a function component', () => {
    expect(typeof Button).toBe('function');
  });
});
